fastapi>=0.115
uvicorn[standard]>=0.30
supabase>=2.6
pyjwt[crypto]>=2.9
python-multipart>=0.0.9
pydantic-settings>=2.4
# Pinned (not >=1.2): the Dockerfile caches `pip install` on this file, so a loose
# pin froze mcp at whatever resolved on the first build — and that older SDK's
# streamable-HTTP route 404s under our /mcp mount. Pinning busts the layer and
# locks the version whose routing we verified (serves at /mcp/).
mcp==1.27.2
httpx>=0.27
